fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
pydantic>=2.5.0
# ASR: Using faster-whisper (faster than standard Whisper)
faster-whisper>=1.0.0
audioop-lts>=0.2.0
aiortc>=1.5.0
av>=11.0.0
# Optional: Keep vosk as fallback if needed
# vosk>=0.3.45
requests>=2.31.0
aiofiles>=23.2.0
numpy>=1.24.0
sounddevice>=0.4.6
pyaudio>=0.2.14
pyttsx3>=2.90
edge-tts>=6.1.0
pydub>=0.25.1
# TTS (Coqui) - optional, install separately if needed: pip install TTS
# Note: TTS may not support Python 3.13 yet
# edge-tts is fast and recommended for Windows (requires internet)

